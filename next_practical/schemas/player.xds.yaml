schema_version: "1.0"

types:
  - name: Player
    type: struct
    fields:
      - name: id
        type: int
        default: 0
      - name: level
        type: int
        default: 0
      - name: name
        type: XString
      - name: items
        type: XVector<int>
