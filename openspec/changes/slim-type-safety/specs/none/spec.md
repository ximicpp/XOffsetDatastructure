## No Spec Changes

This change is a pure implementation-level refactor (diagnostic code and comment trimming).
No public API behavior or requirements are added, modified, or removed.

All existing specs remain unchanged.